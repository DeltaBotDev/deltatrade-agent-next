{
  "name": "agent-next-boilerplate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy && make-agent deploy -u https://agent-api.deltatrade.ai"
  },

  "dependencies": {
    "@delta-trade/core": "^0.0.7"
  },
  "devDependencies": {
    "@types/node": "^20",
    "concurrently": "^9.1.0",
    "eslint": "^8",
    "eslint-config-next": "14.2.15",
    "eslint-plugin-next-on-pages": "^1.13.7",
    "make-agent": "0.0.25",
    "typescript": "^5",
    "wrangler": "^3.99.0"
  }
}
